apiVersion: v1
kind: Service
metadata:
  name: mission-control-backend
  namespace: mission-control
spec:
  selector:
    app: mission-control-backend
  ports:
    - port: 8000
      targetPort: 8000
      name: http
