apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: cloudflared
spec:
  endpoints:
    - dnsName: "external.hoytlabs.app"
      recordType: CNAME
      targets: ["TUNNEL_ID_PLACEHOLDER.cfargotunnel.com"]